import styled from "styled-components";
import { Fonts, Space } from "ui";

export const StyledTitleH1 = styled.h1`
  margin-bottom: ${Space.SuperLarge};
  margin-top: ${Space.SuperLarge};
  margin-left: 64px;
  ${Fonts.H1}
`;
